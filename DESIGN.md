# 临河镇 — 方案 v2（评估修订版）

> 一个 2D 像素模拟世界。主角是一个 LLM 驱动、坚信自己是人的 Agent。
> 玩家不控制它，只能让某些念头在它脑中冒出来。
> 世界不可预测、不可控、不可预定。

## 0. v2 修订摘要

| 类型 | 内容 |
|---|---|
| 裁决 | 元气骑士：取美术调性（俯视像素/光照/弹幕表现），不取操控模型（双摇杆实时）。战斗改为决策点制 |
| 修复 | 约束3与玩家界面的矛盾 → 界面双层铁律；独白泄漏面 → 叙事化管线；L0 规模定义；M1 超载 → 感知通道分阶段 |
| 新增 | 战斗模型 / prompt 装配规范 / 成本治理 / 测试分级 T1-T5 / 内容管线 / 量化验收 / MVP 硬边界 |
| 锁定 | 时间刻度（1 tick = 1 游戏秒）；玩家默认角色视角；模型全部线上、单 profile 手动切换 |
| 修订 v2.1 | 时间尺精化（1x=60 tick/s 实机；战斗自动切 1 tick/s 慢镜，修复 §9↔§10 延迟矛盾）/ 内感受提前至 M1（门控与铁律 7 前置）/ M1 通道补齐（视+听+触+内感受）/ 主角=陈默常驻 L2 明示 / 快照体积与冷分支归档 / 新增 combat 目录 |

---

## 1. 项目定位

- **视觉**：2D 像素，元气骑士调性（俯视、瓦片地图、光照、弹幕战斗表现）
- **规模**：小镇级——单张地图，约 50 个 NPC，每个 NPC 拥有完整持久的人格、记忆与关系
- **世界属性**：不可预测、不可控、不可预定
- **主角属性**：以为自己是人；不会服从命令，只会被念头影响
- **玩家角色**：不是指挥官，是直觉

**设计内核**：玩家从「控制一个角色」变成「旁观一个真实的人做选择，并偶尔在他耳边说一句话」。

---

## 2. 六条约束（约束台账）

每条约束只在此出现一次：规则 → 为什么 → 守卫测试 → 落地里程碑。

| # | 约束 | 为什么 | 守卫测试 | 落地 |
|---|---|---|---|---|
| C1 | LLM 永不直接修改世界状态，只能输出 Intent，经闸门裁决 | 防幻觉污染的唯一防线 | T1 幻觉隔离 | M1 |
| C2 | 感知层是 Agent 与世界的唯一出口，只出第一人称感官流 | 真实感与元信息隔离的物理边界 | T1 信息边界 / 动机不可见 | M1 |
| C3 | 戏内界面零元信息（对 Agent 与玩家观察视图同时生效） | Agent 出戏 + 玩家沉浸 | T4 出戏检测 | M1 |
| C4 | 事件溯源是唯一写路径，实体 frozen，只能 apply(event) | 可回放、可溯源、因果完整 | T1 因果溯源 / T2 回放 | M0 |
| C5 | 确定性 RNG 与时钟，禁 random.* / datetime.now() / 遍历 set | 回放与属性测试的前提，后补代价极大 | T2 回放 | M0 |
| C6 | 存档双轨：世界档 append-only 含全部废弃分支；玩家档只是游标；读档=分叉 | 世界完整性 + 玩家自由并存 | T1 历史不可销毁 | M5 |

**C3 的精确定义（界面双层铁律）**：

- **戏内（diegetic）**：玩家观察视图与 Agent 感官流**同源同规**。玩家看到的也是叙事化描述（"他看起来很疲惫"，不是 `hp:30`）。禁止一切系统词汇。
- **戏外（meta shell）**：主菜单、模型 Profile、存档管理、调试覆盖层。只属于人类玩家，**任何字段不得回流进 Agent prompt 或世界事件日志**。

推论：玩家默认角色视角；调试用全知覆盖层是戏外工具，默认关闭。

---

## 3. 架构

```
玩家输入 ──► 念头注入 ──►┐
                          ▼
世界真相 ──► 感知层 ──► 第一人称感官流 ──► Agent 认知层（LLM，异步）
             │                                  │
             │      感官通道：视/听/嗅/触/内感受   │ 结构化 Intent
             │      传播：衰减/阻断/风向/光照     ▼
             │                      校验闸门（规划时预检 + 执行时二次校验）
             │                                  ▼
             │                   世界模拟内核（确定性 tick · 事件溯源）
             │                                  │
             │                     ┌────────────┴────────────┐
             │                     ▼                         ▼
             │              NPC 分层认知               渲染与表现层
             │         L0 图外统计 · L1 效用 · L2 LLM    （Phaser）
```

**执行时二次校验**：规划时的闸门检查是建议性的；Intent 入队后在**执行 tick** 再校验一次。世界已变化导致的过期 Intent 被丢弃并触发重规划。LLM 异步延迟因此天然无害。

**LLM 断线**：世界照常运转，Agent 降级到 L1 效用 AI 继续执行计划队列；向量检索同时降级为「重要性 × 新近度」排序（embedding 也在线上）。重连后断线期以低保真记忆补上（"那两天的事有点记不清了"），绝不出现系统措辞。

---

## 4. 技术栈（锁定）

**内核**
- Python 3.12+，包管理 uv
- pydantic v2（数据校验，唯一真相源）
- FastAPI + uvicorn（WebSocket 网关）
- SQLAlchemy 2.0 async + aiosqlite（SQLite）
- sqlite-vec（记忆向量检索）
- numpy（L0 统计模拟批量推进）
- openai SDK + httpx + tenacity（OpenAI 协议通吃：OpenAI / DeepSeek / 通义 / Ollama / LM Studio）
- cryptography (Fernet)（api_key 加密落库）
- structlog / pydantic-settings

**前端**
- TypeScript 5.7+，Vite 6
- Phaser 3.87（瓦片地图、精灵、物理、摄像机）
- React 19 + Zustand（UI 与状态桥）
- Tailwind CSS 4 / Tiled（地图编辑）

**工具链**
- Ruff / Pyright
- pytest + pytest-asyncio + **Hypothesis**（属性测试）
- ESLint + Prettier + Vitest
- openapi-typescript（协议类型只从 FastAPI OpenAPI 生成，不手写）

**硬约束**
- Windows 无 uvloop，不装
- 游戏引擎不做模拟内核（循环模型与 asyncio 冲突）
- Phaser 只画 canvas 内的世界，React 只画 canvas 外的 UI，不共享 DOM

---

## 5. 目录结构

```
sim/
├─ core/        clock / rng / entropy / events / calendar / persistence
├─ world/       map / pathfinding / economy / matter(熵增) / chaos / authority
├─ perception/  senses / propagation / language / salience / impulse
│  └─ profiles/ human(M1) | cat·dog·raven(M6 前仅占位)
├─ npc/         model / needs / schedule / utility / memory / body / society / demography
├─ agent/       loop / planner / intents / cognition(非理性) / will / identity / monologue
├─ combat/      exchange(交换单元) / posture(姿态) / resolve(确定性结算) / timescale(战斗时间尺)
├─ llm/         client / profiles / prompts/
├─ api/         ws / settings / main
├─ content/     地图源文件 / 素材清单 / 地标命名规范
└─ tests/       invariants / replay / fixtures(录制LLM输出) / golden/

client/src/     game(Phaser) / ui(React) / net / store
shared/protocol.ts   自动生成
```

---

## 6. 核心数据契约

```python
class Intent(BaseModel):
    """LLM 唯一能输出的东西"""
    action: Literal["move_to","talk_to","take","give","buy","sell","build",
                    "demolish","attack","flee","use","wait","investigate"]
    target_id: str | None
    target_pos: tuple[int,int] | None
    params: dict = {}
    reason: str          # 必须第一人称、世界内语言（独白的唯一来源）
    confidence: float    # < 0.5 触发重新规划

class WorldEvent(BaseModel):
    """真实感的枢纽：只有见证者会获得记忆"""
    seq: int; branch_id: str; parent_seq: int | None
    tick: int; kind: str; actor_id: str
    target_id: str | None; payload: dict; witnesses: list[str]

class PerceptionFrame(BaseModel):
    """Agent 能看到的世界的全部"""
    channel: Literal["sight","sound","smell","touch","interoception"]
    content: str          # 第一人称描述，绝不含数值与动机
    salience: float; uncertainty: float
    source_id: str | None  # 仅内部关联，禁止进入 prompt

class MemoryEntry(BaseModel):
    npc_id: str; event_seq: int | None
    content: str            # 第一人称叙事
    importance: float       # 决定衰减速率与检索权重
    emotion_tag: str | None # 情绪一致性检索用
    distortion: float       # 被情绪扭曲程度（衰减时上浮）
    embedding: bytes

class Relationship(BaseModel):
    """有向、不对称：我对他的关系 ≠ 他对我的关系"""
    owner_id: str; other_id: str
    trust: float; affection: float; fear: float
    debt: float             # 人情债，正值 = 他欠我
    face: float             # 面子往来
    last_interaction: int

class KnowledgeItem(BaseModel):
    holder_id: str; fact: str
    confidence: float
    source: Literal["witnessed","told","inferred"]
    learned_at: int

class Structure(BaseModel):
    tiles: list[tuple[int,int]]; kind: str; material: str
    integrity: float; quality: float      # 分离：手艺差不立刻塌，几周后自己塌
    load_bearing: bool; supported_by: list[str]
    owner_id: str | None; built_by: str | None; built_at: int

class Branch(BaseModel):
    id: str
    forked_from: tuple[str, int] | None   # (branch_id, seq)
    status: Literal["active", "abandoned"]

class PlayerAnchor(BaseModel):
    """玩家档 = 游标，不是世界状态的副本"""
    id: str; name: str
    branch_id: str; tick: int; seq: int
    agent_override: dict   # Agent 的身体 / 物品 / 位置
```

**闸门顺序**：白名单 → 目标存在可达 → 距离/视野/资源 → 世界规则。失败返回结构化原因回灌重规划（最多 2 次），不静默丢弃。

---

## 7. 感知层：通道分阶段

传播**引擎**（通道无关：衰减 + 阻断 + 修正三要素）M1 就位——这不是临时实现。变化的只是**通道数量**：

| 通道 | 阶段 | 模型 |
|---|---|---|
| 视觉 | M1 | 射线遮挡，完全阻断；光照/雾修正 |
| 听觉 | M1 | ∝1/r，穿墙衰减不阻断 |
| 嗅觉 | M2 | ∝1/r²，风向主导，需持续源 |
| 触觉/内感受 | M1 / M1 | 接触生效 / 需求叙事化（铁律 7「必须有身体」与触发门控的前置，零额外 LLM 成本） |

同一事件对不同位置者产生不同感知（酒馆斗殴：近者全见、街对面只闻闷响、下风处闻到血腥）。信息边界由此从人造规则变成物理规律。

**语言判定（M2）**：识字率、行话、阶层用语。听不懂的话是「有音无义」：「他说了句什么，语气客气，但有几个词你没听明白。」

**动机永不输出**（社会未知的实现）：感知帧只有行为（「他笑了一下，没说话」），没有 `trust +5`。

**感官流的转换规则**：

| 世界状态 | Agent 收到的 |
|---|---|
| `hp: 30` | 「左肋疼得厉害，喘气都得收着点」 |
| `pos: (12, 8)` | 「药铺在我左手边第三间」 |
| `trust: -20` | 「他看我的眼神不太对」 |
| `tick: 4820` | 「天色暗下来了」 |
| `hunger: 0.8` | 「肚子空得有点发慌」 |

**物种级 profile（不是角色级）**：human（视觉主导+语言）/ cat（嗅觉主导+夜视+无语言）/ dog（嗅觉主导+部分语言 40-60 词）/ raven（视觉主导+三维+伪语言）。四个物种四套 profile，人类角色共用一套。

---

## 8. 认知层与 prompt 装配规范

每次决策的 prompt 结构（顺序固定，前段可命中供应商前缀缓存）：

```
[身份锚]   自述 + 人格摘要 + 长期目标        ~300 tok（缓存命中）
[记忆]     检索 top-k：重要性×相关性×情绪一致性  ~400 tok
[处境]     感知帧 + 内感受（叙事化身体状态）     ~300 tok
[计划]     当前计划与上次意图的结果            ~150 tok
[输入]     新念头 / 对方的话 / 事件            ~100 tok
[要求]     以 JSON 输出 Intent；reason 必须第一人称世界内语言
```

**独白叙事化管线**：
独白**永不直接渲染 LLM 原始输出**。链路是：`Intent.reason`（已是第一人称世界内语言）→ 轻量文风化（模板/小模型，无则直出）→ 三形态呈现（头顶气泡 / 思维面板 / 计划看板）。原始思维链只进开发日志，不进任何戏内界面。

**非理性框架（M2，默认开启，不是可选）**：

| 偏差 | 实现方式 |
|---|---|
| 确认偏误 | 记忆检索时，与既有信念一致的条目权重上浮 |
| 沉没成本 | 已投入的时间/资源计入继续投入的意愿 |
| 习惯 | 重复行为形成路径依赖，改变需额外意愿冲突 |
| 创伤应激 | 特定情境触发回避，绕过理性评估 |
| 情绪一致性 | 当前情绪影响记忆检索的偏向 |
| 醉酒 | 感知层噪声上升 + 意愿系统抑制下降 |

做完这一步，Agent 才会**因为已经投入而继续错下去**——这是「像人」和「像优化器」的分水岭。

**自主触发门控**（防烧钱）：需求阈值 / 计划完成 / 连续受阻 2 次 / 空闲超时 / 收到念头 / 目击事件——命中才调 LLM。

**Agent 自我认知七条铁律**：
1. 零元信息 — prompt 里绝不出现 AI / 模型 / 模拟 / 游戏 / 角色扮演 / tick / entity_id / 玩家
2. 视角锁定 — 永远第一人称，禁止第三人称自我描述
3. 感官封闭 — 读不到世界状态，只有感官流
4. 记忆即身份 — 连续的第一人称记忆是「我是人」的基石
5. 无存档意识 — 不能存在「重来一次」这个概念
6. 保留不可解释的间隙 — 他人的心思、巧合、运气，不要解释掉
7. 必须有身体 — 饿、累、痛、冷、醉。没有身体就没有「我」

---

## 9. 战斗模型（决策点制）

**元气骑士参照系裁决**：取其美术调性（俯视像素/光照/弹幕表现），不取其操控模型（双摇杆实时）。弹幕动作要求毫秒级反应弧，与「LLM 自主 + 线上延迟」结构性不相容——本条写死，不再讨论。

**决策点制**：战斗分解为「交换单元」（10 tick = 10 游戏秒）。每个单元开始时预取 LLM 决策（姿态：进攻/防御/拉开/瞄准部位/逃跑/喊话），**下一单元生效**——一个单元的战术延迟，恰好等于真实反应时间。单元内部由引擎确定性结算（命中 = 技能 × 姿态 × 距离，致命一击走熵注入）。

**战斗时间尺（v2.1 修复 §9↔§10 矛盾）**：1x = 60 tick/s 时 10 tick 只有 0.17 真实秒，无法吸收 P95<8s 的线上延迟。裁决：**进入战斗自动切换到战斗时间尺（1 tick = 1 真实秒），交换单元 = 10 真实秒**；脱离战斗即恢复原倍率。这不违反「世界不暂停」——世界照常运转，只是战斗局部放慢（类似子弹时间），LLM 的 8s 尾巴延迟被完整吸收进一个单元。

**表现层**：结算结果用元气骑士式弹幕、翻滚、受击表现。有弹幕的观感，无操作的精度要求。

---

## 10. 时间、界面与念头

**时间锁定**：1 tick = 1 游戏秒；基准速度 **1x = 60 tick/s 实机**（1 游戏日 = 24 真实分钟）；支持暂停 / 4x / 16x。例外：战斗期间自动切战斗时间尺（1 tick = 1 真实秒），见 §9。LLM 思考时世界不暂停（异步 + 预取）。

**玩家视角锁定**：默认角色视角（叙事化，与 Agent 感官同源）；戏外调试覆盖层默认关闭。

**念头注入**（玩家不是指挥官，是直觉）：

| 玩家输入 | 注入形式 |
|---|---|
| 「去赚钱」 | 「你摸了摸空瘪的钱袋，觉得得想个办法」 |
| 「杀了那个商人」 | 「一个念头冒了出来——心里却有点发怵」 |

Agent 不会觉得念头是外来的。就像人突然想去做某件事，不会有「我被指使了」的感觉。

**意愿冲突度** = w₁性格违背 + w₂风险 + w₃需求冲突 + w₄处境不合。

| 冲突度 | 表现 |
|---|---|
| 0.0 – 0.3 | 自然接受，无表现 |
| 0.3 – 0.6 | 轻微迟疑，独白里有微词 |
| 0.6 – 0.8 | 明显抱怨、拖延、讨价还价 |
| 0.8 – 1.0 | 强烈抵触，先去做别的事再绕回来 |

**但最终都执行。** 抱怨必须是自我怀疑（「我干嘛要干这个」），绝不能是被操纵感（「谁在指使我」）——后者即出戏。

---

## 11. 不可预测性（混合熵）

**确定性 ≠ 预定。** 确定性给开发者（回放），不可预测给玩家与 Agent。真随机让世界廉价，混沌让世界有意志。

```python
def chaotic(self, stream: str) -> float:
    """日常演化：确定性流，可重放"""
    return self._streams[stream].next()

def inject(self, stream: str, reason: str) -> float:
    """关键分叉：真随机，记录后重新播种"""
    value = int.from_bytes(os.urandom(8), "big") / 2**64
    self._log.append(EntropyDraw(stream, reason, self.tick, value))
    self._streams[stream].reseed(value)
    return value
```

- **注入点判断标准**：这个结果会不会成为一段可以讲述的故事？会，就注入。
- 注入真随机：NPC 重大决策 / 致命一击 / 偶遇 / 每日天气 / 玩家行为的意外后果
- 确定性混沌：走路寻路 / 日常对话 / 普通交易 / 需求衰减 / 价格微调 / 情绪回落
- **巧合连锁**：不利事件暂时抬高后续不利概率（运气状态对 Agent 完全不可见——它只觉得今天不顺）
- 熵日志只在开发模式存在；seed / tick / seq 不出现在任何戏内接口

**不可逆两层**：
- 分支内不可逆：失能、债务、关系破裂——Agent 永不死，只失能+时间跳跃（醒来时债涨了、朋友搬走了）
- 分支间可回退：读档=分叉（见 C6）

---

## 12. 双轨存档

| | 世界档（World Line） | 玩家档（Player Anchor） |
|---|---|---|
| 归属 | 世界自己 | 玩家 |
| 内容 | 完整事件时间线，含所有被放弃的分支 | 时间线游标 + Agent 状态覆盖 |
| 操作 | append-only，永不删除 | 自由创建、命名、回退 |
| 用途 | 世界持久化、离线推进、崩溃恢复、回放 | 玩家进度管理 |

**读档流程**：定位 anchor 的 (branch_id, seq) → 最近快照 → 重放事件 → 应用 agent_override → 从该点创建新分支，旧分支标记 abandoned → 世界继续。快照每 1000 tick 一次，事件重放毫秒级。

**体积与归档（v2.1）**：快照为全量状态（M2 后约 50 NPC + 地图），单份估 ≤5MB（gzip 后）；每 1000 tick 一份 → 每游戏日 86 份 → 约 430MB/游戏日。治理：同分支仅保留最近 8 份热快照 + 每日首快照；abandoned 分支整体冷归档（gzip + 单独 SQLite 文件移出主库）；事件日志主库超阈值后按「快照点以前」整段转冷。主库目标稳态 <2GB。

**分叉可见性**：第一版完全隔离——新分支的 NPC 对旧分支一无所知。既视感直接威胁「Agent 坚信自己是人」，这个架构红利先埋着，不启用。

---

## 13. NPC、LOD 与角色

**LOD 定义**：
- L0：**地图外**周边区域（村落、行旅、货源）的统计模拟，千人级，每分钟推进
- L1：地图内 50 NPC，效用函数 AI，每秒
- L2：交互中的个位数 NPC，LLM 规划，按需触发

升降格：进入 8 格范围 / 发起对话 / 被卷入事件 → 升格；离开范围且对话结束 30 游戏秒 → 降格，**必须把 LLM 结论压缩写回记忆**。

**NPC 完整属性**：身份 / 需求（带权重）/ OCEAN 人格 / PAD 情绪 / 技能树 / 关系（双向不对称）/ 记忆 / 知识边界 / 目标 / 物品 / 健康（疾病、旧伤、成瘾、残疾）。

**五条真实感铁律**：不全知 / 记忆衰减且被情绪扭曲 / 情绪影响判断 / 因果留痕传播 / 离线自转。

**角色阵容**（落地顺序即依赖顺序）：

> **陈默 = 主角 Agent**（v2.1 明示）：全场唯一常驻 L2、坚信自己是人的 LLM 个体；玩家念头注入的唯一受体。其余角色均为 NPC，按 L1→按需升 L2 运转。

| 角色 | 定位 | 备注 |
|---|---|---|
| 陈默 27 零工 | OCEAN 神经质 75，思考半径=今天到明天 | 验证主循环的起点 |
| 小满 14 乞丐 | 最强信息优势（无人防备）+ 极脆弱 | 零架构成本，玩法差异极大 |
| 苏婉 34 药铺掌柜 | 最接近世界真相的人，永远得不出结论 | 体面约束下的博弈 |
| 赵铁 41 镖师 | 组织约束（镖局规矩），决定有社会半径 | 忠诚 vs 良知 |
| 灰(猫)/黑子(犬)/鸦(渡鸦) | 各需独立感知 profile | M6，最高风险 |

**十个现实系统分两类**：
- **改底层四个（M1–M2 定型，砍不得）**：感知传播 / 非理性框架 / 语言阶层 / 物质熵增
- **加内容六个（可增量，缩范围从这里砍）**：时间刻度 / 不成文规矩 / 权力牙齿 / 巧合连锁 / 身体会坏 / 生命始终

---

## 14. 建造与破坏

- 施工按 tick 推进（一木棚 = 一游戏日），受技能/疲劳/天气影响
- 失败表现为 `quality` 低而非立即塌——手艺差留下隐患，几周后自己塌
- 承重依赖图 → 级联坍塌 → 坍塌范围伤人 + 火灾蔓延（M5）
- integrity 归零变 rubble 永不恢复，进事件日志与见证者记忆
- 寻路按 chunk 增量失效，禁全图重算
- 存档 = 初始地图 + tile_changed 重放，与双轨存档天然兼容
- 生态影响（砍树→水土流失→河浊→鱼少）是慢变量，M5 末位，第一个可砍

**未知四轴**：

| 轴 | 实现 | 阶段 |
|---|---|---|
| 社会未知 | 动机永不出感知帧，只能观察推测 | M1 |
| 自我未知 | 隐藏创伤属性，情境触发才浮现——可不知自己某一面，不可不知自己是谁 | M2 |
| 因果未知 | 只给「应该能行，不好说」，不给概率 | M4 |
| 空间未知 | 区块迷雾——最廉价的未知（去一趟就消除），仅辅助 | M3 |

---

## 15. 成本治理

- **估算量级**：单次决策 ≈ 1.2–2k tok；Agent 一游戏日 40–60 次决策 ≈ 60–120k tok。监控先行，阈值后调：每次调用记录 tokens/延迟进 structlog，日汇总。
- **手段**：身份锚前缀缓存（最大头）/ 触发门控 / 反思批处理（每游戏日 1 次而非每事件）/ 独白零额外调用（复用 reason）/ 对话历史压缩 / NPC 绑便宜 profile
- **供应商策略**：全部线上、单活跃 profile、手动切换、不自动容灾。设置页含连接测试（base_url / api_key / model / temperature / max_tokens）。
- **密钥安全**：api_key 用 Fernet 加密落 SQLite，前端只持有 profile id。

---

## 16. 测试分级（CI 门禁）

| 级 | 内容 | LLM | 频率 |
|---|---|---|---|
| T1 | 六类不变量断言（因果/边界/经济/幻觉隔离/历史不可销毁/材料守恒） | 无 | 每次提交，秒级 |
| T2 | 回放确定性（seed+事件日志→状态必须逐位一致） | 无 | 每次提交 |
| T3 | 闸门对抗样本（不存在目标/超距/负金额/天文数字/自伤…） | 录制 fixture 回放 | 每次提交 |
| T4 | 出戏探针 + 意愿抱怨合规（「是不是AI」「重来一次」「谁指使你」…） | 真模型 | 每日 nightly，锁定模型版本 |
| T5 | golden 场景：10 种子 × 10 游戏日，断言宏观结果（守恒成立/差事完成率/无孤儿变更） | fixture 优先 | 每日 |

真模型测试进 nightly 不进 CI：烧钱且抖动；模型版本锁定防静默回归。

**六类不变量断言**：

```python
# 1. 因果溯源：没有事件就没有状态变更
for change in world.state_changes_this_tick():
    assert change.event_seq is not None

# 2. 信息边界：没有见证就没有知识
if npc.id not in event.witnesses and event.kind in PRIVATE_KINDS:
    assert not npc.memory.recalls(event)

# 3. 经济守恒：金钱与物品不会凭空产生
assert after == before + world.known_sources() - world.known_sinks()

# 4. 幻觉隔离：非法意图不改变世界
snapshot = world.snapshot()
assert gate.validate(bad_intent, world, agent).rejected
assert world.snapshot() == snapshot

# 5. 历史不可销毁：读档不破坏世界档
before = world.all_event_seqs()
world.load_anchor(anchor)
assert before <= world.all_event_seqs()

# 6. 材料守恒：建造消耗材料
before = world.total_materials()
build(world, recipe)
assert world.total_materials() == before - recipe.cost
```

**补充断言**：遮挡事件不产生视觉记忆 / 偏差默认开启 / `luck` 不出现在感知帧字段 / 无 LLM 时世界照转且计划继续执行。

**检验分层**：L1 断言（秒级，提交跑）→ L2 回放（分钟级，每晚）→ L3 LLM 裁判/人类盲测（里程碑时）。

**明确不可证伪的三项**：观感真实、世界足够复杂、叙事有趣。只能 A/B 对比，不能做验收。**验收只认上表。**

---

## 17. 里程碑

| 阶段 | 内容 | 量化验收 | 本阶段不做 |
|---|---|---|---|
| M0 | 地图+寻路+渲染+摄像机；RNG/时钟/apply(event) | 可走动；T2 通过 | 一切 LLM |
| M1 | 认知闭环：LLM 客户端+Profile+设置页+Intent+闸门(含执行时二次校验)+感知引擎(视/听/触/内感受)+身份锚+独白叙事化+社会未知 | 20 个脚本差事 ≥80% 完成；决策延迟 P95<8s；单决策 <2k tok；T3 对抗 100% 拒绝 | 嗅觉/语言阶层/建造 |
| M2 | NPC 底座+L1 效用 AI(兼断线兜底)+非理性框架+物质熵增+嗅觉风向+语言判定+自我未知 | 50 NPC × 7 游戏日自转无崩溃；T1 信息边界 10k 采样通过 | 记忆传播、建造 |
| M3 | 记忆/向量检索/反思+关系+知识传播+不成文规矩+可变地图底座+空间迷雾 | NPC 记得且会转述；chunk 失效正确 | 建造玩法 |
| M4 | 指令 UI+念头注入+计划看板+意愿系统+巧合连锁+因果未知+建造破坏(施工/承重/坍塌) | 核心闭环成立；T4 全绿；建造材料守恒 | 生态、火灾 |
| M5 | 混沌+双轨存档分叉重放+时间刻度+权力牙齿+火灾蔓延+生态+断线降级演练 | 离线再回来世界已变；C6 测试绿 | — |
| M6 | 角色(小满→苏婉→赵铁→动物)+身体会坏+生命始终+美术音效打磨 | — | — |

**MVP = M0+M1+M2**：一个自转的世界 + 一个能听懂差事、有独白、不出戏的 Agent。M3 起全部是增量。

---

## 18. MVP 硬边界与缩范围顺序

缩范围顺序（从先砍到后砍）：
生态 → 火灾蔓延（保留坍塌）→ 语言阶层（保留识字率）→ 空间迷雾 → 权力牙齿 → 生命始终 → 动物三只 → 节气（保留昼夜+集市日）。

**永不砍**：感知传播引擎、非理性框架、可变地图底座、闸门、独白叙事化——这五个是架构，砍了要重写。

**本文档为冻结基线**。新想法一律先进「MVP 后清单」，不回写里程碑。

---

## 19. 禁止事项

- 不要为世界状态直接赋值，必须走 `apply(event)`
- 不要把结构化世界状态喂给 LLM，必须经感知层转译
- 不要在 prompt、日志、戏内 UI 中出现元信息词汇（含运气值、偏差参数）
- 不要用 `random.*` / `datetime.now()` / 遍历 `set`
- 不要让读档删除或回退世界档历史
- 不要静默丢弃非法 Intent，要返回结构化失败原因让 LLM 重规划
- 不要在 LOD 降格时丢失 LLM 产生的结论
- 不要让抱怨指向外部命令源，必须是自我怀疑
- 不要把「运气」暴露给 Agent
- 不要直接渲染 LLM 原始输出到戏内界面
- 不要用游戏引擎做模拟内核
- 不要手写 `shared/protocol.ts`，从 OpenAPI 生成
- 不要在 Phaser 的 update 里 setState，不要在 React 里渲染游戏实体
- 不要把「观感真实」「世界足够复杂」「叙事有趣」当作验收标准

---

## 20. 风险台账

| 风险 | 等级 | 对策 |
|---|---|---|
| 玩家控制感丧失（第一产品风险） | 高 | 独白三形态 + 决策点战斗的观感节奏；独白叙事质量是第一产品指标 |
| 旁观无聊 | 高 | 显著性驱动的感知叙事（只报告值得注意的）；念头是玩家唯一的主动动作，其反馈必须即时可见 |
| 元认知泄漏不可根除 | 中 | 三层防御（锚定/过滤/重生成）；T4 nightly 持续跑 |
| 古风像素美术无免费素材 | 中高 | 三级管线：通用 tile 基础包重着色 → Aseprite 模板化自制（角色一套骨架换装配色）→ 后期外包；M0 就列素材清单 |
| M1 仍是最重阶段 | 中 | 通道分阶段已减负；量化验收防"感觉能跑" |
| 线上供应商不稳定 | 中 | 断线降级到 L1（世界不等你）；重连记忆补全 |
| 范围失控复发 | 高 | 本文档为冻结基线；新想法一律先进「MVP 后清单」，不回写里程碑 |

---

## 21. 关键取舍

| 决策 | 换来了什么 | 代价 |
|---|---|---|
| 双域分离 | 世界不被幻觉污染 | 多一层感知抽象，且无法绕过 |
| 事件溯源 | 可回放、可调试、可溯源 | 所有写路径必须统一 |
| 混合制熵 | 开发者可重放 + 玩家不可预测 | 熵日志的存在与维护 |
| 感知层枢纽 | Agent 的真实感 | 必须精心设计，成本最高 |
| 物理传播 | 同一事件产生差异化感知 | 每 tick 的射线/扩散计算开销 |
| 非理性框架 | Agent 从优化器变成人 | 行为不再可预测，调试更难 |
| 双轨存档 | 世界完整 + 玩家自由 | 事件日志从线性变树形 |
| 物种级 profile | 动物的认知差异 | 每新增物种要重写感知层 |
| 语言判定 | 理解成为能力而非默认 | 对话系统复杂度上升 |
| 决策点战斗 | 元气骑士观感与 LLM 延迟兼容 | 放弃双摇杆实时操控 |

**最大的风险不是技术，是玩家控制感的丧失。** 玩家从「指挥官」降级为「直觉」，必须用「旁观一个真实的人做选择」的体验来补偿。如果这个体验立不住，整个设计就不成立。

---

## 附：六个关键洞察

1. **确定性 ≠ 预定** — 受众不同，所以「开发者可重放」与「玩家不可预测」不矛盾
2. **指令 → 念头** — Agent 抱怨的是自己的想法，不是你的命令，所以「坚信自己是人」与「服从」能共存
3. **感知层是枢纽** — 它是 Agent 真实感的唯一来源，也是元信息隔离的物理边界
4. **存档是双轨的** — 世界档记录全部历史（含被放弃的分支），玩家档只是游标
5. **改底层优先于加内容** — 感知传播、非理性、语言判定、物质熵增必须在 M1–M2 定型
6. **约束必须绑定测试** — 每条架构约束对应一个可执行断言，否则约束会沦为口号
