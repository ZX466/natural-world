# Dependencies
__pycache__/
*.py[cod]
node_modules/
.venv/
venv/

# Build artifacts
dist/
build/
*.egg-info/
# tsc -b 增量编译产物（kilo K03 请求：不入库；`tsc --noEmit` 不产生，`-b` 才产生）
*.tsbuildinfo

# Environment & secrets
.env
.env.*
# 例外：示例文件只含键名不含值，必须入库（否则安全清单 G4 不可执行）
!.env.example
*.local.json
credentials.json

# Caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
__coverage__/
coverage/

# Performance benchmark artifacts (pi 裁决：基线与 runner 信息入 docs/perf/ 入库；每日 JSON 不入库)
perf/*.json
.benchmarks/

# Editor / OS
.vscode/
.idea/
Thumbs.db
.DS_Store

# Game runtime data
saves/
logs/
*.log
*.db
*.sqlite

# Dot-folders: per user rule all dot-prefixed agent-config folders stay out of git EXCEPT .orca.
# If the repo was seeded before this rule, untrack with: git rm -r --cached <dir>
# （用户规则 #4，2026-09-20：agent 配置目录不入 git 也不推送；但本地须存在——
#   所有工作树都要有 .codegraph 与 .agents，.claude/.opencode/.codex 只在其对应工作树，主工作树全有）
.agent/
.agents/
.claude/
.codex/
.kiro/
.opencode/

# CodeGraph index (local per-machine database)
.codegraph/

# .orca is the exception (user rule #4): the workflow contract files ARE tracked.
#   tracked:   .orca/workflow.txt, .orca/agent-registry.md
#   ignored:   .orca/talking.txt   (per-worktree local inbox -> avoids cross-branch conflicts)
#   ignored:   .orca/memory.md     (用户规则 #7，2026-09-20：各 agent 记忆，每工作树一份本地保存；
#                               与 talking.txt 同理忽略——否则 5 个分支各自新增同路径文件，收编必冲突)
.orca/talking.txt
.orca/memory.md
