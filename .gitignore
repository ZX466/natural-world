# Dependencies
__pycache__/
*.py[cod]
node_modules/
.venv/
venv/

# Build artifacts
dist/
build/
*.egg-info/

# Environment & secrets
.env
.env.*
*.local.json
credentials.json

# Caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
__coverage__/
coverage/

# Editor / OS
.vscode/
.idea/
Thumbs.db
.DS_Store

# Game runtime data
saves/
logs/
*.log
*.db
*.sqlite

# Dot-folders: all stay out of git per user rule (except .orca which is also ignored).
# If the repo was seeded before this rule, untrack with: git rm -r --cached <dir>
.agents/
.claude/
.codex/
.kiro/
.opencode/

# Orca workflow files (user rule: .orca stays out of git)
.orca/
